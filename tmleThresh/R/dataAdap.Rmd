---
title: "dataAdapt"
output: html_document
---

```{r}
devtools::document()

library(sl3)
library(hal9001fast)
```



```{r}

#rmultinom(n = 2, c(1,2,3,4,5), rep(1/5,5))
rmults <- function(n, size, prob) {
  size = size[1]
  prob = prob[1]
  apply(rmultinom(n = n, 1:size, rep(prob, size))==1, 2, which)

}
rmults(2, c(5,5), c(1/5,1/5))
```

```{r}


library(simcausal)
D <- DAG.empty()
D <- D +
  node("W1", distr = "rmults", size = 15 , prob = 1/15) +
  node("W", distr = "rconst", const = W1 + 1) +
  node("S", distr = "runif",  min = -5, max = 5) +
  node("Y", distr = "rbinom", size =1 , prob = plogis((S ))) + 
    node("pY", distr = "rconst",const = plogis((S )))


setD <- set.DAG(D)
dat <- sim(setD, n = 1500)
dat

```


```{r}
library(data.table)
cutoffs <-  quantile(dat$S, c(0.05, 0.1, 0.2, 0.4, 0.6))

npsem <- list(define_node("W", "W", c()),
              define_node("A_learned", c("S"), c("A")),
              define_node("A", c("S"), "W"),
              define_node("Y", "Y", c("A")))
dat
task <- tmle3_Task$new(as.data.table(dat), npsem)
lrnr_bin <- make_learner(Lrnr_glm_fast)
cv_lrnr <- make_learner(Pipeline, Lrnr_cv$new(make_learner(Lrnr_glm)))

factor_list <- list(W = LF_emp$new("W"), A_learned = LF_fit$new("A_learned", cv_lrnr, type = "mean"))
likelihood <- Likelihood$new(factor_list)
likelihood <- likelihood$train(task)
data.table(likelihood$get_likelihood(task, "A_learned", fold_number = "validation"), task$get_tmle_node("A"))
generator_R <- learner_marker_task_generator(learned_marker_node = "A_learned", node = "Y")

generator_A <- learner_marker_task_generator(learned_marker_node = "A_learned", node = "A")

lf_R <- LF_fit_derived$new("Y", Lrnr_cv$new(Lrnr_thresh$new(lrnr_bin, 
                                    strata_variable = "A",
                                    cutoffs = cutoffs)), likelihood, generator_R)
lf_A <- LF_fit_derived$new("A", Lrnr_cv$new(Lrnr_CDF$new(lrnr_bin, 
                                   10,  cutoffs)), likelihood, generator_A)
task$get_regression_task("Y")$data
likelihood$add_factors(list(R = lf_R, lf_A))
```

```{r}
rev <- generator_A(task, likelihood)
rev$revere_fold_task("validation")$data
data.table(likelihood$get_likelihood(task, "A_learned", fold_number = "validation"))
as.data.table(likelihood$get_likelihood(task, "A", fold_number = "validation"))
```

```{r}
task$get_regression_task("Y")$data

generator <- learner_marker_task_generator()
lf_derived <- LF_derived$new("R", Lrnr_thresh$new(make_learner(Lrnr_glm), 
                                    strata_variable = "A",
                                    cutoffs = cutoffs), )
```


